# Feature TODO

这些是中长期待办，不一定是当前最高优先级，但希望后续实现时有统一落点。

## 候选功能

### 消息显示优化
- 要点
  - 发送新消息时，将当前消息列表清屏（历史消息被顶上去，避免占用用户可见区域，往下滑仍然能滑回去）

### workspace功能
- 目标：为了避免中间产物散乱&互相影响，当前session任务独立执行，不受其他任务影响
- 抉择：是默认就为每个session生成workspace，还是等到需要读/写文件还才触发

### 支持多模态附件
- 支持多模态


## BUGFIX

### create_artifact卡片优化
- 流式加载优化，loading动效，避免加载闪烁
- 高度校准，避免内容和webview底部高度差
- UI规范优化，产出风格统一，更优雅的展示方式
- 支持react产物、以及内置ui工具包（低优先级）

### 语音功能优化
- 目前已有语音功能，但是使用方式以及效果还差强人意